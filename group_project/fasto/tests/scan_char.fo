fun [char] concat([char] x, [char] y) = {x[0],y[0]}

fun [char] writeChar([char] n) = write(n)

fun [[char]] main() =
  let a = {"1", "2", "3"} in
  let b = scan(concat, "0", a) in
  map(writeChar, b)